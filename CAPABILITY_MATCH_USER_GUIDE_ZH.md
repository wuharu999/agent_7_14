# 机器人场景可行性与能力匹配工作台：简明使用手册

这项功能用于回答一个实际问题：**针对某个客户场景，指定机器人目前能否完成，需要补充哪些能力，以及大约需要多少研发投入？**

工作台地址：<http://47.239.12.206:8000/capability-match>

## 一、最快使用方法

1. 打开工作台。
2. 在 **语言** 中选择 `简体中文`。
3. 在 **机器人型号** 中选择要评估的机器人。
4. 在 **客户场景** 中输入任务、环境、负载、速度和验收要求。
5. 点击 **开始分析**。
6. 等待结果出现，然后查看三栏报告。
7. 点击 `Markdown` 或 `PDF` 下载报告。

## 二、怎样描述客户场景

信息越具体，结果越有用。建议复制下面的模板填写：

```text
场景：
环境：室内或室外、地面情况、光照、温度、人流等
任务：机器人需要完成什么动作
物体或负载：重量、尺寸、材质、数量
速度或节拍：每分钟多少次、允许多长时间完成
人员：周围是否有人，是否允许人工协助
限制条件：空间、安全、网络、设备接口等
验收标准：怎样才算成功
```

示例：

```text
在室外公园的爆米花摊位，让 Walker S2 把爆米花定量装入纸杯并递给顾客。
纸杯和工具总负载小于 1.5 kg，目标速度为每分钟 15 杯。
现场有人流和变化光照，允许工作人员在异常时接管。
验收标准：连续完成 30 杯，装填量误差不超过 10%，不能碰撞顾客或打翻纸杯。
```

不要只输入“机器人能不能做服务工作”之类的宽泛问题。至少说明具体任务、环境和成功标准。

## 三、两种进入方式

### 方式 A：直接使用工作台

打开 `/capability-match`，选择机器人和语言，输入场景后点击 **开始分析**。

### 方式 B：从知识问答页面进入

1. 打开系统首页 `/`。
2. 选择一个具体机器人。
3. 打开 **需求分析模式**。
4. 在问答框中描述客户场景并发送。
5. 正常回答完成后，点击 **打开可行性匹配工作台**。

这种方式适合先通过多轮问答补充需求，再生成正式评估。

## 四、怎样看分析结果

### 1. 左栏：场景与需求

左栏显示系统从客户描述中提取的场景、业务目标、环境、负载、作业节拍和原子需求。

先检查这些内容是否与客户原意一致。如果关键数字或限制不正确，请修改场景描述后重新分析。

### 2. 中栏：能力匹配矩阵

中栏逐条显示每个需求与机器人现有能力的匹配情况：

- 绿色：有较强证据支持。
- 黄色：有条件满足，或仍需测试确认。
- 红色：不满足、证据不足，或存在研发缺口。
- `confidence` / `置信度`：系统对当前判断的把握程度，不等同于真实测试成功率。
- `Hard gate` / `硬性门槛`：必须通过的条件；失败时不能判定该需求可行。

如果出现 `R&D Gap (Composite Skill Missing)`，表示机器人可能有底层驱动或接口，但缺少可以直接完成客户任务的完整技能。这不是网页错误，而是明确的研发缺口。

### 3. 右栏：结论与研发投入

重点查看以下内容：

- **技术结论**：从技术角度是否具备实现条件。
- **部署结论**：是否已经适合实际客户现场使用。
- **所需研发投入**：估算的总人周及涉及的研发领域。
- **剩余风险**：当前仍可能导致失败的问题。
- **下一项实验**：最小、最值得优先执行的验证工作。

常见结论可按下面理解：

| 结论 | 简单解释 |
| --- | --- |
| `feasible` | 现有证据表明技术上可行。 |
| `feasible_with_conditions` | 满足列出的条件后，技术上可行。 |
| `prototype_required` | 需要先做原型或实验，不能直接承诺交付。 |
| `infeasible` | 当前条件下不可行。 |
| `viable` | 具备实际部署条件。 |
| `viable_with_conditions` | 完成指定准备工作后可以部署。 |
| `business_case_incomplete` | 技术方案或成本收益信息不足，暂时不能判断是否值得部署。 |

## 五、下载和分享报告

分析完成后，页面顶部会显示两个下载按钮：

- `Markdown`：适合继续编辑、放入项目文档或发送给工程团队。
- `PDF`：适合打印、汇报或直接发送给客户和管理人员。

下载报告时会使用工作台当前选择的语言。切换为 `简体中文` 后再下载，即可获得中文标题和栏目名称。

## 六、管理员如何处理能力缺口

管理员可打开 `/admin/capabilities`：

1. 查看客户场景中反复出现的能力缺口。
2. 查看缺口出现次数、涉及机器人和估算研发投入。
3. 为需要跟进的缺口创建能力草稿。

创建草稿只会建立“待取证”的跟进记录，不会直接修改或删除在线知识库内容。

## 七、常见问题

### 点击开始分析后提示 Worker 离线

表示后台 Worker 没有连接。请联系系统管理员检查 Worker 进程和网络连接。

### 分析结果显示“未知”或证据不足

表示知识库中没有足够的型号、版本、性能或测试证据。不要把“未知”理解为一定不支持，也不要把接口存在理解为已经可以稳定完成任务。

### 结果与客户需求不一致

检查输入是否缺少环境、负载、节拍或验收标准。补充具体信息后重新分析。

### 切换语言后，部分专业名词仍是英文

产品名、机器人型号、SDK/API 名称、能力 ID 和固定技术分类会保留原文，以避免翻译后产生歧义。

## 八、提交客户承诺前的检查清单

- 已选择正确的机器人型号。
- 场景、环境、负载和节拍与客户描述一致。
- 每个硬性门槛都已通过或有明确的验证计划。
- 没有把 `prototype_required` 当作已经可以交付。
- 研发人周只是初步估算，已经由工程团队复核。
- 剩余风险和下一项实验已经写入客户或内部计划。
- 对外发送前，已经人工检查 Markdown 或 PDF 报告。
