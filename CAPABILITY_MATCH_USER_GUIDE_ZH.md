# 机器人场景可行性与能力匹配工作台：简明使用手册

这项功能用于回答一个实际问题：**针对某个客户场景，指定机器人目前能否完成，需要补充哪些能力，以及大约需要多少研发投入？**

工作台地址：<http://47.239.12.206:8000/capability-match>

## 一、最快使用方法

1. 打开工作台。
2. 在 **语言** 中选择 `简体中文`。
3. 在 **机器人型号** 中选择要评估的机器人；如果客户没有指定机器人，保留 **Auto-select from scenario**，系统会根据场景和机器人资料自动选择并显示结果。
4. 在 **客户场景** 中输入任务、环境、负载、速度和验收要求。
5. 点击 **开始分析**。
6. 状态胶囊会明确显示“工作中”“等待回答”“重新连接”“已暂停（Worker 离线）”或“报告完成”。后台分析时可以安全刷新或离开页面。
7. 再次打开同一个评估页面时，系统会继续显示当前进度或已经完成的结果。
8. 点击 `Markdown` 或 `PDF` 下载报告。

## 二、怎样描述客户场景

信息越具体，结果越有用。建议复制下面的模板填写：

```text
场景：
环境：室内或室外、地面情况、光照、温度、人流等
任务：机器人需要完成什么动作
物体或负载：重量、尺寸、材质、数量
速度或节拍：每分钟多少次、允许多长时间完成
人员：周围是否有人，是否允许人工协助
限制条件：空间、安全、网络、设备接口等
验收标准：怎样才算成功
```

示例：

```text
在室外公园的爆米花摊位，让 Walker S2 把爆米花定量装入纸杯并递给顾客。
纸杯和工具总负载小于 1.5 kg，目标速度为每分钟 15 杯。
现场有人流和变化光照，允许工作人员在异常时接管。
验收标准：连续完成 30 杯，装填量误差不超过 10%，不能碰撞顾客或打翻纸杯。
```

不要只输入“机器人能不能做服务工作”之类的宽泛问题。至少说明具体任务、环境和成功标准。

## 三、两种进入方式

### 方式 A：直接使用工作台

打开 `/capability-match`，选择机器人和语言，输入场景后点击 **开始分析**。

### 方式 B：从知识问答页面进入

1. 打开系统首页 `/`。
2. 如已知机器人，可先在机器人下拉菜单中选择；否则保留“全部机器人”。
3. 在问答框中输入场景草稿（可选）。
4. 点击 **评估机器人匹配度**，直接进入 `/capability-match`。
5. 场景分析只在专用工作台运行；主页仍然只处理普通知识问答。

这种方式会携带当前草稿、语言和已选择的机器人；未选择机器人时由工作台自动选择。

## 四、怎样完成澄清并查看报告

### 1. 每次回答一个关键问题

系统先确认客户目标，以及从触发到结束的完整工作流。之后每次只询问一个可能改变安全、可行性、架构、成本或验收结论的问题。

如果暂时不知道答案，可选择“我还不知道”。系统会要求指定保守假设或验证负责人，避免场景永久卡住。

### 2. 稳定后自动分析

目标和工作流通过最低门槛，且没有仍由客户决定的结论性问题后，页面显示自动分析倒计时。也可以提前分析，但报告会明确标记未解决条件。

进度连接中断时页面会自动重连；刷新或暂时离开不会终止后台任务。

### 3. 报告抽屉

分析完成后，页面底部报告抽屉会继续保留在同一场景中。重点查看：

- **结论与成立条件**：当前判断及其适用边界。
- **主要证据**：支持状态、能力类型和证据摘要。
- **高影响未知项**：负责人以及解除未知状态的下一步。
- **下一项实验**：最小、最值得优先执行的验证工作。
- **历史版本**：每份报告固定绑定生成时的场景版本。

常见结论可按下面理解：

| 结论 | 简单解释 |
| --- | --- |
| `fit` | 现有证据表明场景与机器人匹配。 |
| `fit_with_conditions` | 满足列出的条件后可以匹配。 |
| `prototype_required` | 需要先做原型或实验，不能直接承诺交付。 |
| `insufficient_evidence` | 当前证据不足，不能判断支持或不支持。 |
| `not_a_fit` | 已知硬性边界与场景要求不匹配。 |

## 五、下载和分享报告

分析完成后，页面顶部会显示两个下载按钮：

- `Markdown`：适合继续编辑、放入项目文档或发送给工程团队。
- `PDF`：适合打印、汇报或直接发送给客户和管理人员。

下载报告时会使用工作台当前选择的语言。切换为 `简体中文` 后再下载，即可获得中文标题和栏目名称。

## 六、管理员如何处理能力缺口

管理员可打开 `/admin/capabilities`：

1. 在页面顶部切换 `English / 简体中文`。
2. 选择机器人型号，查看“自上次成功整理以来的原始文件变更”。窗口会分别列出上传或新增、内容修改、软删除所造成的文件路径变化，并显示当前 Wiki 证据文件数量。
3. 首次点击 **整理新增变更** 时，Worker 会自动扫描整个生成的 `wiki/`（排除 `wiki/capabilities/`，避免把整理结果再次当作证据）。Python 会实际读取每个符合条件的文本文件，按固定字节大小分批交给无工具的 LLM 提取；LLM 不能自行挑选或跳过文件。建立成功基线后，普通整理只读取新增或修改的 Wiki 页面。
4. 中断后继续时点击 **恢复全量扫描**，系统会复用内容与当前流水线版本完全匹配的检查点。需要重新判断全部证据时点击 **强制全量重新提取**；它会忽略现有检查点，让每个批次重新调用 LLM。流水线版本升级也会自动使旧检查点失效。
5. 在“共享整理进度”中查看排队、已完成批次、检查点恢复数、候选数、被阻塞文件和排除原因。这个状态保存在 ECS 数据库中，不属于某一个浏览器；刷新页面或由另一位管理员打开页面时都会看到同一个任务。
6. 在“已整理的原子能力”表格中查看能力编号、名称、可观察效果、证据数量和状态。

单个提取批次最多允许 DeepSeek 运行 30 分钟，最终合并最多允许 60 分钟，完整任务最多允许 24 小时。如果某一批超时，之前成功的批次仍保存在内容哈希检查点中；使用“恢复全量扫描”只重新运行未完成的部分。每个提取批次与最终合并只会使用当前步骤所需、经过白名单筛选的原子能力策略章节。
7. 整理成功后，经过结构验证的草稿会以原子方式写入 `wiki/capabilities/<机器人型号>/`。旧目录会先备份；已审核或已验证条目不能被自动覆盖。
8. 页面下方仍可查看客户场景中反复出现的能力缺口、工程投入等级，并创建“待取证”的能力草稿存根。

整理器不会执行删除、弃用或把草稿提升为已验证状态。它读取 LLM Wiki 已生成的文本证据，不直接要求 DeepSeek 解码原始 PNG/JPEG。每个成功批次会按内容哈希保存检查点，中断后的重试可以复用已完成批次。若 Wiki 证据仍有阻塞或未处理项，页面会显示“完成但有警告”，并且不会更新成功基线；这些变化会保留到下一次整理。

## 七、常见问题

### 为什么分析需要几分钟

一次分析需要先把客户场景拆成原子需求，再从知识库查找当前机器人有证据支持的原子能力，最后执行硬性门槛、风险和研发投入评估。它比普通知识问答更复杂。

页面显示 **正在分析** 代表后台任务仍在运行，不是网络通信失败。此时可以刷新页面、打开其他页面或稍后回来；请不要因为等待时间长而重复点击 **开始分析**。

如果页面短暂无法取得状态，会自动重新连接，不会立即把分析标记为失败。只有后台任务返回明确失败结果时，页面才显示失败。

### 点击开始分析后提示 Worker 离线

表示后台 Worker 没有连接。请联系系统管理员检查 Worker 进程和网络连接。

### 分析结果显示“未知”或证据不足

表示知识库中没有足够的型号、版本、性能或测试证据。不要把“未知”理解为一定不支持，也不要把接口存在理解为已经可以稳定完成任务。

### 结果与客户需求不一致

检查输入是否缺少环境、负载、节拍或验收标准。补充具体信息后重新分析。

### 切换语言后，部分专业名词仍是英文

产品名、机器人型号、SDK/API 名称、能力 ID 和固定技术分类会保留原文，以避免翻译后产生歧义。

## 八、提交客户承诺前的检查清单

- 已选择正确的机器人型号。
- 场景、环境、负载和节拍与客户描述一致。
- 每个硬性门槛都已通过或有明确的验证计划。
- 没有把 `prototype_required` 当作已经可以交付。
- 研发人周只是初步估算，已经由工程团队复核。
- 剩余风险和下一项实验已经写入客户或内部计划。
- 对外发送前，已经人工检查 Markdown 或 PDF 报告。
