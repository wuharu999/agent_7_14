# 优必答知识库 (Uchat Knowledge Base) 用户使用手册

本手册详细介绍了**优必答知识库系统**的设计架构、用户角色权限、日常操作规范及各项高级功能的使用方法。

---

## 1. 系统简介

优必答知识库是一款基于双机架构的智能问答与知识库管理系统：
* **ECS网关服务器**：负责提供公共访问入口、用户身份鉴权、多语言翻译及 WeCom（企业微信）回调服务。
* **Worker计算节点**：负责保存原始文档、运行 LLM Wiki 知识导入引擎、以及调取本地 Claude AI 命令行进行问答检索。

---

## 2. 用户角色与权限划分

本系统目前仅支持以下**两个核心角色**，去除了传统的观察者（Viewer）角色，以确保企业知识产权的安全。

| 角色类型 | 可访问页面 / 接口 | 核心权限范围 |
| :--- | :--- | :--- |
| **编辑者 (Editor)** | `/` (问答)<br>`/manage` (文件管理)<br>`/upload` (文档上传与AI编写) | 提问检索、上传文档、查看获授权的文件树、软删除文件/目录、进行交互式文档编写、导出知识库。 |
| **管理员 (Admin)** | `/` (问答)<br>`/manage` (文件与机器人管理)<br>`/upload` (文档上传与AI编写)<br>`/admin/users` (用户管理) | 拥有编辑者所有权限，以及创建机器人、分配编辑者、创建用户、重置密码、修改权限范围、启用或禁用账户。 |

---

## 3. 核心功能操作指南

### 💡 功能一：智能知识问答 (`/`)
1. **选择范围与语言**：
   * 在页面顶部卡片的 **机器人 (Robot)** 下拉菜单中选择提问的对象机器人（例如全部、`tian_gong`、`walker_s2` 等）。
   * 在 **回答语言 (Language)** 下拉菜单中选择目标语言（支持简中、繁中、韩语、日语、英语等 8 国语言）。页面整体的 UI 会自动同步切换到对应语言。
2. **提交提问**：
   * 在文本输入框中输入您的问题，按回车或点击 **提问** 按钮发送。
   * 当系统正在检索和生成答案时，右下角的**机器人助理 (PetDex Companion)** 会切换为“思考/充电”状态。页面只显示面向用户的答案，不显示 Claude 的内部检索或思考过程。
3. **开始新对话**：
   * 点击 **新对话** 按钮会重置当前浏览器的会话 ID 并清空聊天记录，方便您开始新的问题探讨。

---

### 📂 功能二：源文件管理与审计 (`/manage`)
编辑者和管理员可通过此页面管理存储在 Worker 节点 `raw/sources/` 下的文件树。

1. **刷新源文件树**：
   * 点击 **刷新源文件树** 以从 Worker 获取最新的目录状态。文件夹支持点击折叠与展开。
2. **软删除（防误删机制）**：
   * 点击文件或文件夹右侧 of **删除** 按钮，系统会提示确认。
   * 确认后，文件或目录将被**移动到垃圾箱 (`.agent1-trash/` 目录)**中保存，而不会被物理抹除。
   * *注意：若文件正处于 LLM Wiki 导入处理状态 (processing)，删除操作将被临时锁定。*
3. **审计日志与分析报告**：
   * 页面底部的 **最近审计日志** 会实时记录登录、文件上传、文件删除等敏感操作。
   * 管理员和编辑者可点击 **生成用户活动报告** 按钮，一键分析系统最近的用户操作轨迹及客户端 IP 地理位置信息。
4. **创建机器人并分配编辑者（仅管理员）**：
   * 创建机器人时，Worker 必须在线。名称仅允许 1–64 位英文字母、数字、下划线或连字符。
   * 系统会先在 Worker 创建对应的 `raw/sources/<机器人名称>/` 文件夹，成功后才写入 ECS 数据库；失败时不会留下只有数据库记录而没有文件夹的机器人。
   * 左侧 **编辑者池** 只显示已启用的 `editor` 账户。将编辑者拖到右侧机器人卡片即可分配权限；点击已分配编辑者旁的 **×** 可移除权限。
   * 创建或调整后，聊天页、企业微信问答页、上传页和源文件树会使用同一份机器人列表。已打开的聊天页需要刷新后显示新选项。

---

### 📝 功能三：文档上传与 AI 编写 (`/upload`)
此页面提供两种文档入库方式：**常规文档上传** 和 **Claude AI 多轮对话编写**。

#### 1. 常规文档上传 (Manual Upload)
* **支持格式**：`.pdf`, `.docx`, `.md`, `.txt`, `.pptx`, `.xlsx`, `.csv`, `.json`, `.html`, `.xml`, `.yaml`。
* **压缩包支持**：支持上传包含上述类型源文件的 **ZIP 压缩包**。如果有多份相互关联的参考文件，请打包成 ZIP 上传以放在同一个源文件夹内。
* **安全机制**：禁止上传含有绝对路径、`../` 路径穿越、特殊系统链接的 ZIP 压缩文件。

#### 2. Claude AI 多轮对话文档编写 (AI Authoring)
当您需要从零开始梳理或补充知识文档时，可以借助本地的 Claude HAiku 模型协助撰写：
1. 点击右上角问号 `?` 按钮可以阅读详细的 ChatGPT/Kimi/飞书对话记录导出指引，以便直接导入外部聊天内容。
2. 点击 **启动新文档编辑会话**。
3. 在下方的对话框中与 Claude 展开多轮对话，向其描述产品知识、故障现象或操作细节。
4. 对话成熟后，点击 **生成 Markdown 文章**。
5. 系统会在页面下方生成最终预览。您可以对文章的标题、Markdown 内容进行手工微调。
6. 确认无误后点击 **发布到知识库**，文章即会自动同步至对应的机器人目录下，并触发 LLM Wiki 的增量导入。

---

### 👥 功能四：用户账户与范围权限管理 (`/admin/users`)
*仅管理员 (Admin) 账户可见，点击顶部导航栏的 **用户管理 (User Management)** 进入。*

1. **创建新用户**：
   * 填写用户名、电子邮箱，并指定初始密码（**密码长度必须大于等于 10 位**）。
   * 在角色下拉菜单中选择 `编辑者 (Editor)` 或 `管理员 (Admin)`。
   * 在下方勾选其**允许访问的机器人文件夹范围**（例如勾选 `tian_gong` 后，该用户将仅被允许在 `tian_gong` 文件夹下执行管理、上传或导出操作）。
2. **编辑用户与重置密码**：
   * 点击列表中任意用户右侧的 **编辑权限/密码**，在弹窗中修改其角色、机器人范围，或重置其密码（留空则保持原密码不变）。
3. **账户启用/禁用开关**：
   * 点击用户状态列的 **已启用/已禁用** 徽章，可一键冻结或恢复该用户的访问权限。
   * *注意：系统会自动阻止您停用您当前登录的管理员账户。*

---

## 4. 🗃️ 知识库数据导出 (Export)

具有 `editor` 或 `admin` 角色的用户可以在问答页（Web版和企业微信版）的输入框下方点击 **导出到我的AI (Export to my AI)** 按钮。系统将把当前知识库的所有 Wiki 静态 Markdown 文档在 Worker 端自动打包成 ZIP 文件并下载到您的本地。
